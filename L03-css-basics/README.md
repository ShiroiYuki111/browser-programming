# Lecture 03 – CSS Basics

## What I implemented this lecture
- Linked an external stylesheet to style my HTML without altering the original structure.
- Implemented typography basics, including font families, line-height, and header sizing.
- Created a centered, constrained layout using max-width and auto margins.
- Styled a horizontal navigation bar and ensured all images are responsive.

## CSS selectors I used (at least 5)
- Element selector (`body`, `h1`, `img`, `section`)
- Group selector (`header, footer`)
- Descendant selector (`nav ul`, `nav a`)
- Pseudo-class (`nav a:hover`)
- ID selector (`#main-content`)
- Class selector (`.card`)

## One thing I struggled with
- Understanding the exact difference between margin (outside spacing) and padding (inside spacing) when building the layout.

## One improvement I want to do next
- I want to learn CSS Flexbox so I can make my navigation and layout even cleaner and more modern.
